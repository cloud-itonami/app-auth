{
  "name": "itonami-app-auth",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "description": "auth.itonami.cloud — Passkey and OAuth PKCE for the itonami app plane (CF Worker, ClojureScript).",
  "type": "module",
  "scripts": {
    "pages": "kbb -M:render-pages",
    "app": "amu compile --target wasm32-browser browser",
    "worker": "amu compile --target wasm32-browser worker",
    "build": "npm run pages && npm run app && npm run worker",
    "dev": "wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "dry-run": "wrangler deploy --dry-run --outdir dist",
    "test": "kbb -M:test",
    "test:worker": "kbb --backend sci test/worker_smoke.cljk",
    "check": "npm run build && npm run test && npm run test:worker"
  },
  "devDependencies": {
    "shadow-cljs": "3.4.11",
    "wrangler": "^4.121.0"
  }
}
